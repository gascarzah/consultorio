# Copiar estas líneas a backend/.env (o al gestor de secretos del nuevo servidor).
# Ajustar host, puerto y contraseña del rol consultorio_app.

DB_URL=jdbc:postgresql://localhost:5432/consultorio_db
DB_USERNAME=consultorio_app
DB_PASSWORD=CambiarEstaContraseña
